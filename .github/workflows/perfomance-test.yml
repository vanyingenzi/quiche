name: Benchmark Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Run benchmark script
        run: |
          cd apps
          bash multicore_benchmark.sh